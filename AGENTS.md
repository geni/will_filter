# Coding Standards

## General
- use Module.prepend instead of monkey-patches
- prefer ' instead of " for quoting strings without interpolation or ' in them

# Testing
- test suites can be run via: bundle exec bin/test <test_file>
- individual tests can be run via: bundle exec bin/test <test_file>:<line_number>

## Git
- exclude claude references in git commit messages

